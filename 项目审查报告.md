# 批量 PDF 发布插件 代码审查报告

审查日期：2026-08-02
审查范围：`BatchPdfPublisher`（net48 + Net8 双工程）、`BatchPdfPublisherLauncher`、图框/扫描/发布/属性批量/目录插入等全部源码。

说明：本次为静态代码审查（无法在本环境编译/运行 AutoCAD）。结论按严重程度分级，标注了对应的文件与行号。

---

## 一、高危问题（会导致功能不生效或明显错误）

### 1. “输出到各 CAD 文件同级目录”选项从未生效
- 位置：`Views/PublisherForm.cs:372`、`ViewModels/PublisherViewModel.cs:131/526/558`、`Services/PdfPublisherService.cs:84-91`
- 问题：界面勾选 `OutputNextToCadFile` 后只是把值写入 `ProjectProfile`，但实际发布路径 `CreateEngineeringOutputFolder()` 只读取 `project.OutputDirectory`，从不判断 `OutputNextToCadFile`。该选项只影响界面提示文字和“打开目录”按钮，PDF 永远输出到 OutputDirectory。
- 影响：README 声称“输出到指定目录，或输出到各 CAD 文件同级目录”，实际后者不生效。
- 建议：在 `PublishPdf`（`PublisherViewModel.cs:790`）处，若 `OutputNextToCadFile` 为真，应针对每个 DWG 生成各自的工程输出子目录，而不是统一使用一个 `engineeringFolder`。

### 2. 发布前检查里的“修改图框登记”按钮必然报“找不到图框登记”
- 位置：`ViewModels/PublisherViewModel.cs:984`
- 问题：`EditValidationIssue` 用 `sheet.Frame`（例如 `"A1"`，这是**纸张规格**）去匹配 `FrameDefinition.BlockName`（这是**图块名称**，如 `图框A1_001`）。两者几乎不可能相等，因此点击“修改图框登记”总是弹出“当前工程没有找到图框登记：A1”。
- 影响：发布前校验对话框中的修改入口不可用。
- 建议：`SheetItem` 应记录图块名（或直接用 `BlockHandle` 反查图框定义），把查找逻辑改为按图块名/句柄匹配。

---

## 二、中危问题（边界条件、健壮性）

### 3. 发布过程中关闭面板会触发已释放控件访问
- 位置：`Views/PublisherForm.cs:391-403`（`ViewModelPropertyChanged`）
- 问题：`PublishPdf` 是 `async void`，发布期间若用户关闭面板，`FormClosed` 会 `Dispose` 视图模型；异步续体继续触发 `Status`/进度属性变更，回调里直接 `_status.Text = ...`（无 try/catch），可能抛 `ObjectDisposedException`，打断剩余发布/合并流程。
- 建议：`ViewModelPropertyChanged` 内先判 `IsDisposed` 并 try/catch；或在发布开始后禁用关闭。

### 4. 图纸来源在布局空间时的打印窗口换算可能偏移
- 位置：`Services/PdfPublisherService.cs:750-765`（`GetPlotWindowInDcs`）
- 问题：把 WCS 图框范围用**当前视图**的 WCS→DCS 矩阵换算成打印窗口。模型空间通常没问题；但布局（图纸空间）的 PlotWindow 原点与 WCS 原点可能不一致，直接按当前视图换算可能导致窗口偏移、裁切不正确。
- 影响：布局图框在部分图纸上可能打印偏移。
- 建议：用布局/图纸空间的 DCS 换算（或直接用图纸空间坐标），并在布局图纸上做一次实际发布验证。

### 5. 图框“版本”签名对几何过于敏感
- 位置：`Services/FrameIdentityService.cs:18-48`（`DefinitionSignature`）
- 问题：签名把块内每个实体几何范围的 3 位小数坐标做 SHA-256。任何轻微移动/旋转/重生成导致的坐标差异都会生成不同签名，从而频繁触发“同名图框版本”询问，或让 `SelectBest` 选不到已登记版本。
- 影响：误判版本、重复登记提示多。
- 建议：签名只取实体类型 + 属性 TAG + 相对比例（长宽比），或对坐标先归一化/量化（例如除以参考尺寸取整）。

### 6. 项目/图框 JSON 写入无异常保护
- 位置：`Services/PublishPlanStore.cs:122-127`
- 问题：`SaveProjects` / `SetActiveProject` 直接写文件，磁盘满、权限错误等异常会向上抛到 UI 事件处理器（多数没有 try/catch），可能导致按钮无响应或崩溃。
- 建议：与其它存储方法一致，内部 try/catch 并记录日志。

### 7. 旧发布路径（Publish/BuildJobs/PlotGroup）是死代码
- 位置：`Services/PdfPublisherService.cs:237-374`
- 问题：`Publish`、`BuildJobs`、`PlotGroup` 没有被任何调用方使用（发布入口用的是 `PreparePages` + `FinalizePreparedPages`；框选发布用 `PublishMerged`）。保留两套发布逻辑容易产生“改了这边、那边没改”的不一致。
- 建议：删除死代码，只保留 `PreparePages/FinalizePreparedPages/PublishMerged` 一条链路。

---

## 三、低危 / 建议

8. **WPF `PublisherControl.xaml` 是未启用的旧 UI**
   - `Views/PublisherControl.xaml:14` 的“发布 PDF”按钮是禁用状态，提示“PDF 发布引擎将在下一开发阶段接入”；实际使用的界面是 WinForms 的 `PublisherForm`。该 WPF 控件没有任何地方引用，属于遗留代码。建议删除或明确标记为弃用，避免误导。

9. **文化相关的数值解析**
   - `Views/FrameCreationForm.cs:109/118` 用 `double.Parse(_height.Text)`（当前区域设置）；在 `,` 小数分隔符的系统上会解析失败。建议统一用 `CultureInfo.InvariantCulture`（目录插入表单已这样做）。
   - `Views/AttributeBatchForm.cs:149` 的容差解析用 `CurrentCulture`，同样建议改 Invariant。

10. **`UniqueDirectory` 秒级碰撞**
    - `Services/PdfPublisherService.cs:1019-1027`：同一秒内两次同名工程发布会复用同一目录（内部文件仍由 `UniquePath` 保证不覆盖，但目录复用可能不符合“每次新目录”预期）。

11. **`Commands` 静态目录插入状态**
    - `Commands.cs:103-146`：`_catalogSheets/_catalogSettings` 为静态字段，多文档/重入场景存在陈旧数据风险（当前 finally 已清理，风险低）。

12. **`FrameCreationService.CreateFrameBlock`（非选择版）为死代码**
    - `Services/FrameCreationService.cs:63-89`：所有属性定义叠放在同一中心点，未被 UI 使用；真实入口是 `CreateFrameBlockFromSelection`。建议删除以免误用。

13. **`MenuService` ActiveX 分支全部是注释掉的死代码**
    - `Services/MenuService.cs:31-61`：`Install()` 直接 `return`，实际用 `LoadPartialMenu`（MNU 加载）。可清理。

14. **天正识别与工程扫描的耗时**
    - `CadCompatibilityService.IsTianzhengDrawing` 与 `FrameRegistrationService.ScanProjectCadFiles` 都会遍历全部实体/全部工程 DWG，大项目下可能明显卡顿。可考虑加缓存或后台化。

15. **构建层面的注意点**
    - 双工程（net48 / net8）共享同一批源码，Net8 工程用 SDK 默认通配会额外包含 `PublisherControl.xaml` 等；只要不用就无碍。
    - 根目录 `PdfSharp.dll` 同时被两个工程引用；Net8 下请确认它是 netstandard2.0 兼容版本，否则需换用对应版本。
    - `BatchPdfPublisher.csproj` 固定 `PlatformTarget=x64`（AutoCAD 2022 为 64 位）合理；但若未来要支持 AnyCPU 加载需注意。

---

## 四、做得好的地方

- 打印链路对“真实纸张尺寸”做了强校验（`ValidateTemporaryPage`/`PdfMerger` 页尺寸比对、阻止错误页进合并文件），并有详细诊断日志。
- 原子写入（暂存 + `File.Replace` + 备份恢复）和磁盘空间预检很扎实。
- PDF 合并在 0 白边时直接导入原页避免二次绘制，内存与保真更好。
- 图框扫描对代理对象/异常块有防御，单个坏块不会中断整个扫描。
- 启动器对 PC3/PMP 毫米纸张库的部署与加长介质注入考虑得比较完整。
- 自动保存用 `Wblock()` 快照 + 原子替换，不阻塞 CAD。

---

## 五、建议优先修复顺序

1. `OutputNextToCadFile` 真正生效（功能缺失）。
2. 发布前校验的“修改图框登记”按图块名/句柄正确查找（入口不可用）。
3. 关闭面板中断发布时的 `ObjectDisposedException` 防护。
4. 布局空间打印窗口换算做实际验证，必要时修正。
5. 清理死代码（旧发布路径、WPF 旧面板、非选择版建块）。
