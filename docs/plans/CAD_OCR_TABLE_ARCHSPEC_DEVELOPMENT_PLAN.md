# CAD 图像识别、CAD 表格转换与建筑设计说明增强开发计划

> 文件用途：后续 Codex 修改和测试的执行依据。  
> 建立日期：2026-09-06。  
> 基准分支：`codex/detail-layout-frame-ranges`。  
> 基准提交：`0726c65`（普通门默认图集迁移为“无”）。  
> 适用范围：`LineVision` 图片转 CAD、独立 CAD 表格转换模块、`CadArchSpecEditor`。
> 关联计划：`docs/plans/CadArchSpecEditor_Codex_MasterPlan.md`。

---

## 1. 总目标

按相互独立、可以逐步交付的方式完成三项能力：

1. 提升“图片转 CAD”的中文、数字、尺寸符号和旋转文字识别能力；
2. 增加“CAD 表格转可编辑表格文件”功能；
3. 参考 GenOffice 的结构化文档和局部修改思想，继续完善建筑设计说明助手。

三项能力共享结构化数据模型和导出能力，但不得把所有识别流程混成同一种算法：

- 图片、扫描件、PDF 底图：使用 OCR 和图像结构识别；
- 原生 DWG：优先直接读取 CAD 实体，不经过截图和 OCR；
- 建筑设计说明：以结构化项目数据为唯一业务数据源，AI 只能提交可审阅的局部修改建议。

### 1.1 与现有门窗立面功能的边界

- 本计划后续功能与门窗立面业务无关；
- 不在 `Features/DoorWindows` 下增加 OCR、CAD 表格或建筑说明代码；
- 不复用门窗表格的业务模型、门窗编号规则、门窗图集设置或门窗 UI；
- 可以借鉴现有天正对象探针、只读爆炸和跨版本处理经验，但应提取为独立通用服务；
- CAD 表格识别结果的主要去向是 CSV/XLSX 和建筑设计说明专业表格。

---

## 2. 不可违反的安全原则

### 2.1 不破坏用户图纸

- 识别、预览、导出阶段只读访问当前 DWG；
- 未经用户明确点击“写回 CAD”，不得新增、删除或修改实体；
- 写回操作使用单独事务，任一关键步骤失败时整体回滚；
- 不用识别结果自动覆盖原文字、原表格或原设计说明；
- 每次写回前显示目标、数量和处理方式。

### 2.2 本地优先

- PaddleOCR 默认在本机独立进程运行，图纸和图片不上传；
- OCR 模型版本固定，下载包必须校验哈希；
- 模型不可用时允许回退到现有 Windows OCR，并清楚显示当前使用的引擎；
- OCR Worker 崩溃、超时或内存不足不得拖死 AutoCAD UI。

### 2.3 结果必须可审查

- OCR 文字保留原文、规范化文字、置信度和坐标；
- CAD 表格识别保留源实体 Handle、所在空间、图层及几何范围；
- 低置信度、跨格、无法归属文字必须单独标记，不得静默丢弃；
- 导入建筑设计说明的数据保留来源和导入时间。

### 2.4 跨版本兼容

- AutoCAD 2022—2024 使用 .NET Framework 4.8；
- AutoCAD 2025—2026 使用 .NET 8；
- OCR 和复杂文件转换放在独立 Worker 中，避免原生依赖进入 AutoCAD 进程；
- 共享协议使用向后兼容的 JSON，并带协议版本号。

---

## 3. 技术选型结论

### 3.1 PaddleOCR 的使用边界

采用 PaddleOCR 作为增强 OCR 与扫描表格识别引擎，主要使用：

- PP-OCR 系列：图片转 CAD 的文字检测与识别；
- 文字方向、旋转框和置信度输出；
- PP-StructureV3：扫描图片或 PDF 中的表格结构识别；
- 后续根据测试决定是否使用单字坐标。

不采用以下做法：

- 不把 Python 运行时或 Paddle 原生库直接加载进 AutoCAD；
- 不让 PP-Structure 处理原生 DWG 线框表格；
- 不自动跟随 PaddleOCR 最新版本，必须锁定已验证版本。

PaddleOCR 为 Apache-2.0 许可。发布时保留许可证、版权声明和第三方依赖清单。

### 3.2 GenOffice 的使用边界

GenOffice 主要用于架构参考，不整体嵌入当前 AutoCAD 插件。重点借鉴：

- 原始文档作为事实来源；
- 结构化块模型；
- 仅更新被修改的块；
- 修改前后快照和差异；
- 文档解析、编辑、渲染、导出相互分离；
- AI 操作产生建议，用户确认后才应用。

不得直接复制 GenOffice 的 `ee/` 企业许可目录。引用其 Apache-2.0 代码时必须逐项记录来源和许可证。

### 3.3 表格文件格式

- 第一阶段支持 CSV，确保简单、稳定、可测试；
- 正式版本增加 XLSX，保留合并单元格、行高、列宽、换行和基本对齐；
- 不使用已废弃的 Excel COM 自动化；
- 优先选择支持 .NET Framework 4.8 与 .NET 8 的开源 OOXML 方案，选型后固定版本并补充第三方声明。

### 3.4 追加调研结论

#### Autodesk 代理对象机制

Autodesk 官方说明表明：

- 第三方自定义实体在定义程序未加载时会以 `ProxyEntity` 形式存在；
- `ProxyEntity.Explode()` 可以返回其保存的图形副本；
- API 的 `Explode()` 是只读分解，不会删除或改写原实体；
- 这与用户执行的 `EXPLODE` 命令不同，后者会删除原对象并把分解结果写入图纸。

因此，天正文字兼容采用“原生属性优先、只读分解后备”，禁止为了读取内容而执行 CAD 的炸开命令。

#### `cad-extract`

开源项目 `ricklove/cad-extract` 提供了值得借鉴的 CAD 表格处理顺序：

1. 识别表格和单元格；
2. 将文字定位到单元格；
3. 识别合并单元格；
4. 分离相邻或接触的多个表格；
5. 输出 CSV/XLSX；
6. 用提取结果重新绘制并进行视觉回放比较。

该项目规模较小且 README 明确列有合并行列等未完成问题，因此只借鉴算法分层和视觉回放测试，不直接作为产品依赖。

Autodesk 在 2026 年的官方支持说明中仍表示 AutoCAD 不能直接把既有线条和文字转换成 Table，只能使用 DATAEXTRACTION 后人工整理，或自行开发自动化。因此“线框与文字自动还原成单元格”确实是需要本项目实现的独立能力，不是对已有 AutoCAD 命令的重复包装。

#### Camelot

Camelot 面向 PDF 而非 DWG，但其“结构由矢量线或图像线检测，单元格内容优先取原始文本层，只有扫描件才 OCR”的原则与本项目一致。可参考其结构与文字分离思想，不引入其 Python PDF 技术栈到 AutoCAD 插件。

#### ezdxf

`ezdxf` 适合独立 DXF 文件读写和测试数据生成，但本产品已经运行在 AutoCAD 内部，DWG 主路径应继续使用 Autodesk API，以保留对象 Handle、块变换、原生 Table 和天正运行环境。`ezdxf` 只考虑用于离线测试工具，不作为 DWG 正式读取器。

---

## 4. 总体架构

```text
图片 / 扫描件 / PDF
        │
        ▼
PaddleOCR 独立 Worker ──► OCR 统一 JSON ──► LineVision 预览和 CAD 写入
        │
        └────────────────► 扫描表格结构结果

原生 DWG
        │
        ▼
CAD 实体读取器 ──► 网格推断 ──► 单元格文字归属 ──► 表格识别预览
                                                        │
                                  ┌─────────────────────┼──────────────┐
                                  ▼                     ▼              ▼
                                CSV                   XLSX       建筑说明专业表格
```

建议新增模块，名称可在实施阶段按现有解决方案结构调整：

```text
PaddleOcrWorker/
CadTableExtraction.Core/
CadArchSpecEditor/src/CadArchSpec.Host.Shared/CadTable/
CadArchSpecEditor/src/.../Imports/
```

如果最终仍由主插件提供 Ribbon/命令入口，入口只负责启动独立功能，不得让 CAD 表格核心依赖 `Features/DoorWindows`。

统一数据协议至少包含：

- `protocolVersion`；
- 输入文件哈希和尺寸；
- 识别引擎及模型版本；
- 文字内容、原始内容、置信度、旋转角和多边形；
- 表格范围、行列边界、合并关系和单元格内容；
- 警告、耗时及错误码。

---

## 5. 分阶段执行计划

状态标记：`[ ]` 未开始、`[~]` 进行中、`[x]` 完成、`[!]` 阻塞。

当前执行优先级（2026-09-06 经用户确认）：

```text
阶段 0 基线
  → 阶段 2 原生 CAD 表格与天正文字
  → 阶段 1 PaddleOCR 图片文字增强
  → 阶段 3 扫描表格
  → 阶段 4 建筑设计说明增强
  → 阶段 5 发布回归
```

编号保留不变，避免历史引用失效；实际执行以以上顺序为准。

### 阶段 0：基线冻结与样本准备

- [x] 记录当前 LineVision、CAD 宿主和建筑说明测试基线；
- [ ] 收集不少于 20 张建筑图像样本，覆盖中文、数字、标高、尺寸、轴号、旋转文字和低清晰度；
- [ ] 收集不少于 15 个 DWG 表格样本，覆盖原生 Table、线框表格、块内表格、旋转表格、断线和合并单元格；
- [ ] 样本脱敏，不把真实项目名称和敏感路径提交到 Git；
- [~] 建立期望结果文件，允许自动比较文字、位置、行列和合并关系；已完成第一批合成规则表格期望测试，真实样本随采集继续补充。

验收条件：

- 基线构建通过；
- 样本类型清单完整；
- 每类至少存在一个可重复自动测试的期望结果。

### 阶段 1：PaddleOCR 增强 Worker

#### 1.1 协议与进程

- [x] 在现有 `ILineVisionOcrEngine` 基础上增加引擎标识和能力信息；当前 Windows OCR 已声明协议版本、语言、旋转、置信度及多边形能力，后续 PaddleOCR 使用同一接口；
- [x] 定义带版本号的 OCR 请求/响应 JSON；请求携带唯一任务编号，客户端仅接受协议版本和任务编号均匹配的结果，旧缓存不会误用；
- [x] 新建 PaddleOCR 独立 Worker；固定 `PaddleOCR 3.7.0 + PaddlePaddle 3.3.1 + PP-OCRv6 small`，支持版本协议、真实文字多边形、置信度和结构化失败；已打包 Python 运行时、依赖与固定模型，用户电脑无需安装 Python；增强 Worker 异常时自动回退 Windows OCR；
- [x] 支持取消、超时、进度、标准错误日志和强制结束；Windows OCR 与 PaddleOCR Worker 均通过版本化 JSON 事件报告校验、模型初始化、图片读取、推理、几何整理和完成进度，客户端同时持续排空标准输出/错误，取消或超时会强制结束独立进程；
- [x] Worker 临时目录使用唯一任务目录，完成后安全清理；
- [x] AutoCAD 关闭时取消当前任务，客户端强制结束超时或取消的 Worker，不无限等待。

#### 1.2 模型与部署

- [x] 选定稳定的中文检测和识别模型：`PP-OCRv6_small_det`、`PP-OCRv6_small_rec`，方向分类使用 `PP-LCNet_x1_0_textline_ori`；
- [x] 测量 CPU 首次启动、重复识别、内存和安装体积；1632×1587 示例图实测首次独立进程 18.817 秒、后两次独立进程平均 18.666 秒、最大峰值工作集 1089.3 MB、发布组件 652.4 MB，三轮均输出 81 个文字区域；应用内相同内容与参数由 SHA-256 缓存直接复用，独立 Worker 每次重载模型的数据作为未命中缓存的最坏路径；详见 `docs/benchmarks/LINEVISION_OCR_PERFORMANCE_2026-09-08.md`；
- [x] 明确发布方式：作为总安装包中的共享可选组件随包携带，位于根目录 `OcrEngine`，不在 R24/R25 中重复保存；
- [x] 增加模型版本、逐文件 SHA-256、组件清单以及 PaddlePaddle、PaddleOCR、PaddleX 许可证；
- [x] 模型缺失或损坏会返回结构化错误并自动回退 Windows OCR；模型随包携带，不在用户电脑临时下载。

#### 1.3 工程图文字处理

- [x] Worker 协议输出真实文字多边形、置信度和方向，LineVision 客户端按多边形接收；
- [x] 增加尺寸字符规范化，同时保留 `OriginalText`；只规范有明确工程语境的字符，所有自动修改均可回看原文；
- [x] 处理尺寸中的 `×/X/*`、`Φ/φ/Ø`、`±/+/-`、全角数字、比例冒号、负号和括号；叙述文字中的“X轴”及普通英文 X 不改写；
- [x] 处理竖排文字和 90/180/270 度旋转；PaddleOCR 以文字框长边确定基线、方向分类区分正向与倒向，预览按真实四点框显示，CAD 插入按基线起点和垂直字高回映；旧几何缓存自动失效；
- [x] 对重叠识别框去重；仅对规范化文字一致且多边形包含率或交并比达到安全阈值的结果合并，保留高置信度框；内容不同或位置分离的文字完整保留；
- [x] 使用多边形而非粗略矩形遮罩文字，避免误删附近线条；传统线段分析和独立矢量 Worker 共用同一四点框遮罩，扩边按多边形轮廓计算，不再清空整个外包矩形。

#### 1.4 UI

- [x] 增加“自动选择 / PaddleOCR 增强 / Windows OCR 兼容”选项，并记忆用户选择；
- [x] 显示当前引擎、模型版本、识别阶段和进度；OCR Worker 的 0—100 分阶段进度映射到整套图片分析进度，完成 OCR 后继续显示线稿分析与矢量化进度，回退 Windows OCR 时也明确提示；
- [x] 允许取消；取消会结束独立 Worker，未完成结果不会进入预览；
- [x] 识别完成后允许逐条编辑、启用或禁用文字；
- [x] 低置信度文字使用明显但不妨碍查看的状态标识；低于用户阈值的文字在列表中以浅橙底色和“待复核”状态显示，预览使用橙色虚线多边形及框外警示标记，默认不生成但允许用户核对后手动启用。

验收条件：

- AutoCAD UI 在识别过程中可移动、可取消、可关闭；
- Worker 异常不导致 AutoCAD 崩溃；
- 在基准图片集上，中文和工程符号正确率明显高于 Windows OCR；
- 原有 Windows OCR 模式仍可独立工作；
- LineVision 现有线、圆、圆弧和墙体识别测试无回归。

### 阶段 2：原生 CAD 表格转可编辑表格

#### 2.1 用户流程

- [x] 新增入口“从 CAD 读取表格”；建筑设计说明助手可识别后新增为可编辑专业表格，总插件另提供 Ribbon/经典菜单/快捷键入口和独立命令 `CAD2XLSX`，无需打开说明窗口即可只读框选并直接导出 XLSX；
- [x] 用户框选一个表格范围或选择原生 Table；线框与文字走几何识别，原生 `Table` 直接读取行列、文字及合并范围；
- [x] 只读分析后显示可编辑预览；识别结果新增为自定义表格并自动打开专业表格编辑器，不覆盖已有内容；
- [x] 用户确认行列、合并单元格和文字；导入后自动打开现有表格编辑器，可改单元格、增删行列、合并或拆分；
- [x] 导出 CSV/XLSX，或导入建筑设计说明专业表格；XLSX 保留中文、换行、表头、边框、冻结首行、筛选和合并范围。

#### 2.2 CAD 实体读取

- [x] 支持 AutoCAD `Table`；采用 `Rows/Columns/Cells/GetMergeRange` 非废弃 API，保留合并单元格跨度；
- [x] 支持 `Line`、`Polyline`、`Polyline2d` 中的水平和垂直边；旧式 `Polyline2d` 顶点也通过事务只读访问；
- [~] 支持 `DBText`、`MText` 和 `AttributeReference`；已完成宿主只读适配，待真实 DWG 验证；
- [~] 支持天正原生文字和天正文字代理对象；已完成 RXClass/IDispatch/只读分解读取链，待 T20/T30 样本验证；
- [~] 正确处理块参照变换、嵌套块、比例和旋转；块参照继续使用 AutoCAD 只读 `Explode` 返回的已变换副本，另补读不会稳定随分解返回的属性参照；整体旋转表格已自动归正，嵌套动态块待真实 DWG 验证；
- [x] 默认忽略不可见、冻结或关闭图层，并允许用户选择包含；“包含隐藏图层”选项会在本机记忆，结果提示本轮忽略的隐藏对象数量；
- [x] 记录每个识别元素的源 Handle，便于定位；单元格汇总边框线和文字 Handle，同时保存来源 DWG，专业表格编辑器可选中单元格后定位并缩放到 CAD 来源对象；

#### 2.2.1 天正文字兼容读取链

天正文字不是一个固定类型，T20/T30、不同 AutoCAD 版本以及“是否加载天正运行环境”会得到不同对象表现。因此不得只判断一个 CLR 类型，按以下优先级读取：

1. **标准文字**：直接读取 `DBText`、`MText`、块属性及原生 Table 单元格；
2. **天正运行环境已加载**：根据 `RXClass.DxfName` 识别 `TCH_*TEXT*`、`TCH_*WORD*` 等候选对象，通过只读 COM/IDispatch 候选属性读取真实文字、位置、高度和旋转；
3. **只读分解**：对无法直接取值的天正实体或 `ProxyEntity` 调用 API `Entity.Explode(DBObjectCollection)`，递归收集临时 `DBText/MText/AttributeReference`；
4. **图形 OCR 后备**：若对象有可见图形但前三层均取不到文字，只在用户允许时渲染选区并 OCR，结果标为低可信来源；
5. **无法识别**：保留源 Handle、DXF 名称和范围，在预览中显示“天正文字待确认”，不得丢弃或凭空生成内容。

实现约束：

- 禁止调用用户命令 `EXPLODE`；
- `Explode()` 返回的临时对象必须释放，不得加入数据库；
- 递归深度和对象数量必须有限制，防止异常代理对象卡死；
- COM/IDispatch 属性采用只读白名单，不调用未知方法；
- 文字坐标必须应用块参照和嵌套对象的完整变换矩阵；
- MText 同时保留纯文本与必要换行，不把格式控制码写入 Excel；
- 记录读取途径：`Standard`、`TianzhengProperty`、`ExplodedClone`、`OcrFallback`；
- 天正未加载时仍要尽量识别代理图形；天正已加载时优先读取真实对象数据；
- 分别建立 AutoCAD 2022—2026 与可获得的天正 T20/T30 样本测试。

现有 `TianzhengDoorWindowService`、`TianzhengScaleService` 和 `TianzhengTitleProbeService` 已验证 RXClass、IDispatch 与只读分解路径可行。实施时只提炼通用读取机制，不让新模块引用门窗业务服务。

#### 2.3 网格推断

- [x] 合并共线短线和小范围断开的边线；
- [x] 建立 X/Y 边界聚类，容差随文字高度确定；
- [x] 判断外框、内部网格、缺边和多余装饰线；已识别闭合外框和缺边，过滤未参与闭合单元格边界的开放装饰线，并在存在唯一主表格时排除不含文字的独立单格装饰框；含文字、结构复杂或无法判断主表格的独立区域全部保留并提示确认；
- [x] 识别横向和纵向合并单元格；以最小闭合外框和缺失的内部分隔线推断，并明确提示用户确认；
- [x] 支持整体旋转后的表格；根据全部线段长度加权推断正交主方向，在副本坐标中归正线框与文字后识别，不修改输入实体或原始检测数据；
- [x] 对不闭合或无法唯一判断的区域给出警告，不把待确认区域伪装成已识别内容。

#### 2.4 文字归属

- [x] 根据文字包围盒中心和重叠面积归属单元格；中心落在边界时按包围盒多数重叠确定，接近均分的跨格文字继续进入待确认；
- [x] 同一单元格内按阅读顺序合并多段文字；
- [x] 保留 MText/天正多行文字的段落顺序，清理 CAD 格式控制码；同一视觉行的分段文字按横向顺序拼接；
- [x] 跨格文字和边界文字进入待确认列表；内容相同且位置重合的直接文字、天正属性文字和分解副本按来源优先级去重，不影响不同单元格中的相同文字；
- [x] 数值按文本保存，避免编号、前导零和日期被 Excel 自动误改。

#### 2.5 编辑与导出

- [x] 复用 `ArchitectureTable` 作为中间数据模型；
- [x] 预览中支持改单元格、插入/删除行列、合并/拆分；
- [x] CSV 使用 UTF-8 BOM；
- [x] XLSX 保留合并、换行、基本对齐和 CAD 原列宽，并按显式换行、估算折行及跨行合并范围计算行高；
- [x] 导出文件名使用表号和表格标题，不使用内部 GUID；
- [x] 导入建筑说明时新增表格，不覆盖已有专业表格。

验收条件：

- 原生 Table 能完整导出；
- 规则线框表格的行列、文字和合并单元格正确；
- 旋转、断线、块内表格不会造成崩溃或错误写图；
- 导出的 XLSX 可由 Excel/WPS 正常打开；
- 整个识别过程不修改当前 DWG。

### 阶段 3：扫描表格和 PDF 底图后备识别

- [x] 在表格工具中增加“图片/扫描表格”来源；首版支持 PNG/JPEG/BMP/TIFF 有线表格，后台调用随包 PaddleOCR 读取文字并从二值像素提取横竖网格，结果统一进入 `ArchitectureTable` 编辑器；识别失败不生成空白或伪正确表格；
- [ ] 接入 PP-StructureV3 有线表格识别；
- [ ] 评估无线表格识别，达不到稳定标准时保持实验功能；
- [ ] 将 PaddleOCR 表格结果转换为统一 `ArchitectureTable`；
- [ ] 同时显示原图、单元格框和可编辑网格；
- [ ] 低置信度单元格必须人工确认后才能导出；
- [ ] PDF 只处理用户指定页和范围，避免一次加载整套大图纸。

验收条件：

- 扫描表格可以生成可编辑网格；
- OCR 失败不会生成看似正确但实际错位的静默结果；
- 原生 DWG 表格仍默认走直接实体解析。

### 阶段 4：建筑设计说明增强

#### 4.1 结构化内容块

- [ ] 明确章节、段落、列表、专业表格、引用和人工锁定块；
- [ ] 每个块具有稳定 ID、来源、修改人/方式、时间和版本；
- [ ] 项目参数与正文、表格之间使用显式绑定；
- [ ] 未修改块在保存和导出过程中保持原样。

#### 4.2 局部修改与差异

- [ ] AI 或规则引擎只能提交局部补丁；
- [ ] 应用前显示新增、删除、替换的内容；
- [ ] 支持逐项接受、拒绝和撤销；
- [ ] 保存前创建版本快照；
- [ ] 人工锁定内容不得被自动修改。

#### 4.3 CAD 与表格数据复用

- [ ] 从 CAD 识别出的表格可作为新表或候选数据导入；
- [ ] 支持将专业表格写入指定 CAD 图框区域；
- [ ] 写回前校验图框 Handle、DWG 路径和可用范围；
- [ ] 来源 CAD 更新时只提示差异，不自动覆盖人工编辑结果。

#### 4.4 文档导出

- [ ] 在现有 CAD 输出基础上规划 DOCX/PDF 输出；
- [ ] 保留标题层级、编号、表格、分页和项目字段来源；
- [ ] 建立渲染后视觉校验，不以“文件能打开”作为完成标准；
- [ ] 不宣称自动替代设计、校对、审核或施工图审查。

验收条件：

- 单个章节或表格可以独立修改并查看差异；
- 未修改内容在保存后不发生无关变化；
- CAD 导入数据具有来源记录和冲突提示；
- 版本恢复能够还原正文、表格和字段绑定。

### 阶段 5：发布、兼容与回归

- [ ] AutoCAD 2022—2026 分别验证启动、识别、取消、关闭和写回；
- [ ] 在 100%、125%、150%、175%、200% Windows 缩放下检查 UI；
- [ ] 测试无 PaddleOCR、模型损坏、中文路径、超长路径和只读目录；
- [ ] 测试大图片、大表格和低内存环境；
- [ ] 安装包包含许可证、模型来源和版本清单；
- [ ] 用户版默认不要求安装 Python、Paddle 或网盘客户端；
- [ ] 建立升级和卸载测试，卸载不得删除用户项目或识别结果。

验收条件：

- 标准安装包可在未安装开发环境的新电脑上运行；
- 所有长任务均有进度、取消和超时；
- 不出现关闭窗口后 AutoCAD 仍被后台任务阻塞；
- 回归测试和手工验收清单全部通过后才进入用户版。

---

## 6. 测试矩阵

### 6.1 图片文字

- 白底黑字、黑底白字和彩色图；
- 低分辨率、压缩噪点、屏幕截图和扫描件；
- 简体中文、数字、英文缩写；
- 横排、竖排、倾斜和倒置；
- 标高、轴号、尺寸串、直径和正负号；
- 文字紧邻线条、文字压线和密集标注。

### 6.2 CAD 表格

- 原生 Table；
- 完整线框、断线、双线和局部缺边；
- 合并表头、跨行和跨列；
- DBText、MText、块属性和多行文字；
- 天正原生单行/多行文字、天正代理文字、天正未加载时的代理图形；
- 同一张表混用 AutoCAD 文字与天正文字；
- 模型空间、布局空间、普通块和嵌套块；
- 旋转角度、非 1:1 块比例和不同图形单位；
- 不规则内容和无法唯一判断的反例。

### 6.3 数据安全

- 识别取消前后 DWG 数据一致；
- Worker 崩溃后 DWG 数据一致；
- 导出失败不产生半成品覆盖正式文件；
- 写回失败事务完整回滚；
- 项目文件恢复后来源和版本记录完整。

---

## 7. 每个阶段的固定工作流程

后续所有修改严格按以下顺序执行：

1. 在本文档中把当前任务改为 `[~]`；
2. 检查工作树，保留用户已有修改；
3. 补充或先写失败测试；
4. 实现最小闭环，不跨阶段顺手重构无关模块；
5. 运行针对性测试；
6. 运行相关模块完整测试和构建；
7. 检查 AutoCAD UI 是否可取消、可关闭；
8. 更新本文档状态、测试结果和已知限制；
9. 生成用户测试包，并明确测试入口和预期结果；
10. 用户确认后再进入下一阶段。

如果实际实现需要偏离本计划，必须先在本文档“变更记录”中写明原因、影响和新方案。

---

## 8. 第一轮实施范围

第一轮只完成“阶段 0 + 阶段 1 的最小闭环”，不得同时开始扫描表格和设计说明 AI 改写：

1. 固定 OCR JSON 协议；
2. 建立 PaddleOCR Worker 原型；
3. 用一组脱敏建筑图进行 Windows OCR/PaddleOCR 对比；
4. 接入现有 LineVision 预览；
5. 保留 Windows OCR 回退；
6. 验证识别期间 AutoCAD 不阻塞。

达到这一闭环后，再开始原生 CAD 表格提取。这样可以分别定位 OCR、CAD 几何解析和表格编辑的问题。

---

## 9. 已知风险

| 风险 | 处理原则 |
| --- | --- |
| PaddleOCR 安装体积较大 | 独立可选组件或首次按需下载，模型固定并校验 |
| CPU 识别速度不足 | 裁剪、缓存、限制输入尺寸，并提供取消 |
| CAD 表格线条不闭合 | 容差聚类加人工确认，不静默补成确定结果 |
| 表格中文字跨格 | 标记冲突，用户确认归属 |
| Excel 自动改变编号格式 | 数值默认按文本导出，需要时再显式转数值 |
| AutoCAD 多版本 API 差异 | 共享核心与薄宿主分离，分别构建测试 |
| OCR 或 AI 修改错误 | 原文、来源、置信度和差异始终可见 |
| 第三方许可证遗漏 | 安装包自动生成第三方声明，禁止引入许可不清依赖 |
| 天正对象随版本变化 | RXClass 分类加多级读取链，不绑定单一 CLR 类型或单一属性名 |
| 天正未加载只能看到代理对象 | 使用只读 `ProxyEntity.Explode()`，失败时明确标记并允许 OCR 后备 |

---

## 10. 变更记录

| 日期 | 变更 | 原因 |
| --- | --- | --- |
| 2026-09-06 | 建立初版计划 | 明确 PaddleOCR、CAD 表格转换和建筑设计说明增强的实施边界与顺序 |
| 2026-09-06 | 收紧模块边界并增加天正文字兼容方案 | 后续功能以建筑说明和独立 CAD 表格转换为主，与门窗立面业务解耦 |
| 2026-09-06 | 调整执行优先级并开始原生 CAD 表格核心 | 用户确认建筑说明和 CAD 表格是主要方向，PaddleOCR 排在原生表格闭环之后 |

### 10.1 执行记录

| 日期 | 工作 | 结果 |
| --- | --- | --- |
| 2026-09-06 | LineVision 基线 | 15 项通过，0 失败 |
| 2026-09-06 | 建筑说明 .NET 基线 | 30 项通过，0 失败 |
| 2026-09-06 | 建筑说明网页基线 | 44 项通过，生产构建通过 |
| 2026-09-06 | AutoCAD 宿主基线 | 2022/net48 与 2026/net8.0-windows 分别编译通过，0 警告 |
| 2026-09-06 | 新建 `CadArchSpec.CadTable` 独立核心 | 规则网格、断线合并、文字归格和冲突保留 3 项测试通过；建筑说明测试总数增至 33 项 |
| 2026-09-06 | 新建通用 CAD/天正只读实体读取器 | AutoCAD 2022、2026 宿主分别编译通过，0 警告 |
| 2026-09-06 | 打通 CAD 表格识别到建筑说明表格编辑器的最小闭环 | 增加“从 CAD 读取表格”和自定义表格类型；框选后只读识别行列与文字并新建预览表格；网页 46 项、.NET 33 项测试通过，两版宿主 0 警告编译通过 |
| 2026-09-07 | 增加 AutoCAD 原生 `Table` 直读 | 用户选中原生表格时直接读取行列、文字和合并范围；避免先分解再猜测，2022/2026 SDK 均以非废弃 API 0 警告编译通过 |
| 2026-09-07 | 增加 XLSX 导出闭环 | 专业表格编辑器可直接另存 Excel；新增无 Office 依赖的 OpenXML 导出核心与中文、合并单元格自动化验证，外部解析器成功打开样本 |
| 2026-09-07 | 增加线框表格合并单元格推断 | 根据最小闭合外框及缺失内隔线识别横向、纵向合并范围；不完整区域继续警告并待人工确认，核心测试增至 37 项 |
| 2026-09-07 | 增加旋转表格与旧式多段线读取 | 整体旋转的正交表格自动归正后识别；块属性和 `Polyline2d` 纳入只读读取链，核心测试增至 38 项，AutoCAD 2022/2026 宿主均 0 警告编译通过 |
| 2026-09-07 | 增加 CAD 表格隐藏图层过滤 | 默认排除关闭、冻结和对象自身不可见的线条/文字，用户可显式勾选包含；选择会本机记忆并回报忽略数量，网页 47 项、核心 38 项测试通过 |
| 2026-09-07 | 打通 CAD 表格单元格来源定位 | 表格模型保留来源 DWG、边框线和文字 Handle；编辑器可定位所选单元格来源，定位前校验当前图纸并按当前视角缩放，网页测试增至 48 项 |
| 2026-09-07 | 修复 CAD 表格多行文字与原图不一致 | 改为从 MText 原始内容解析段落和可见文字，天正属性文字复用同一清理链；同一视觉行碎片先横向拼接，多行单元格使用可换行编辑框，并按 CAD 原列宽比例显示；核心测试增至 42 项，网页 48 项通过，AutoCAD 2022/2026 宿主均 0 警告编译通过 |
| 2026-09-07 | 完成 XLSX 列宽与多行行高输出 | CAD 原列宽换算为 Excel 列宽；显式换行和估算折行自动增加行高，跨行合并单元格把所需高度分配到覆盖行；核心测试增至 43 项 |
| 2026-09-07 | 修复 Excel 打开导出文件时提示修复 | 将 `autoFilter` 调整到 SpreadsheetML 规范要求的 `mergeCells` 之前并加入顺序回归测试；用户实际错误文件由 Microsoft OpenXML 校验器稳定检出 1 项，修正后代表性多行表格为 0 项错误 |
| 2026-09-07 | 修复 XLSX 合并区域外边框缺失 | 合并范围内被覆盖的单元格不再从 XML 省略，而是写入空值和边框样式，保证 Excel 可组合出完整外边线 |
| 2026-09-07 | 完成 CAD 文字包围盒重叠归格 | 标准文字、MText、块属性和天正文字记录实际几何包围盒；整体旋转时同步变换包围盒；边界文字按多数重叠归格，接近均分时仍留待人工确认；核心测试增至 45 项 |
| 2026-09-07 | 完成重叠重复文字安全去重 | 仅当可见内容相同且包围盒高度重叠或中心足够接近时去重，优先保留直接 CAD、天正属性、分解副本和 OCR 中更可靠的来源；相同文字位于不同单元格时完整保留；核心测试增至 47 项 |
| 2026-09-07 | 增强开放装饰线过滤 | 基于已形成单元格实际使用的边界二次收敛网格，排除表外开放引线、下划线和表内短线，不修改源 DWG；独立闭合装饰框继续保留待人工确认；核心测试增至 49 项 |
| 2026-09-07 | 完成独立闭合装饰框安全过滤 | 将闭合单元格按共享边界划分连通区域；存在唯一主表格时仅排除不含文字的独立单格框，含文字、结构复杂或主表格不明确的区域均保留并提示确认；核心测试增至 51 项 |
| 2026-09-08 | 完成 LineVision 旋转文字方向与 CAD 回映 | PaddleOCR 根据四点框长边及 180 度方向分类输出 0/90/180/270 度方向；预览改画真实多边形，CAD 使用基线起点和真实垂直字高；识别缓存加入几何版本，实测 90/180/270 度均通过，LineVision 测试 24 项、R24/R25 编译通过 |
| 2026-09-08 | 完成 LineVision OCR 重叠框安全去重 | 基于凸多边形真实交叠面积判断，仅合并文字一致且高度重合的识别结果并保留高置信度框；重叠异文和同文不同位置均保留，避免误删相邻尺寸标注 |
| 2026-09-08 | 完成 LineVision 多边形文字遮罩 | 传统线段识别、骨架中心线和轮廓矢量化统一按 OCR 四点框填充并沿轮廓扩边；旋转框外但外包矩形内的像素不再被擦除，减少文字附近线条误删 |
| 2026-09-08 | 完成 LineVision 低置信度文字复核提示 | 以用户设置的最低置信度为判断阈值；文字列表增加“正常/待复核”状态与浅橙底色，预览增加橙色虚线四点框和框外警示三角，低置信度项默认不生成但可人工启用；完整构建中 LineVision 28 项、楼梯 87 项测试通过，R24/R25 均 0 警告编译 |
| 2026-09-08 | 完成 OCR Worker 分阶段百分比进度 | OCR 协议升级至 v2，进度事件同时校验协议版本与任务编号，忽略第三方日志和其他任务事件；Windows/Paddle Worker 均报告内部阶段，客户端持续排空输出避免管道阻塞，并将 OCR、线稿分析和矢量化映射为连续总进度；冻结后的真实 Worker 端到端测试 31 项通过 |
| 2026-09-08 | 完成 PaddleOCR 发布组件性能基准 | 增加可重复运行的 CPU 基准脚本；i5-12400F 对 1632×1587 样图三轮均识别 81 项，首次 18.817 秒、重复独立进程平均 18.666 秒、峰值工作集 1089.3 MB、组件 652.4 MB；明确独立进程数据是缓存未命中最坏路径 |
| 2026-09-09 | 完成 CAD 表格独立命令和直接 XLSX 导出入口 | 新增 `CAD2XLSX` 及总插件 `WLCAD2XLSX` 包装入口，Ribbon、经典菜单和快捷键设置自动显示“CAD表格转Excel”；复用原生 Table/线框/文字/天正只读识别与既有 XLSX 导出，不打开建筑说明窗口、不修改源 DWG；核心测试 51 项通过，R24/R25 宿主及总插件均 0 警告编译 |
| 2026-09-09 | 扫描图片表格来源第一版 | 建筑说明表格工具增加“从图片读取表格（实验）”；支持 PNG/JPEG/BMP/TIFF，PaddleOCR 在独立进程读取文字，二值像素提取长横竖线后复用现有闭合网格、合并单元格及文字归格核心；识别在后台执行且按钮可主动取消，关闭窗口也会终止 Worker；所有结果标记待人工复核，无表格线、无文字、Worker 错配或失败均不生成表格；核心测试增至 52 项 |
| 2026-09-07 | 完成 LineVision OCR 版本化协议基础 | OCR 引擎新增稳定标识和能力信息；客户端与独立 Worker 改用带协议版本、唯一任务编号的 JSON 请求/响应，拒绝旧缓存和错配结果；总插件发布构建完成 18 项 LineVision 端到端测试，并确认最新版建筑说明模块已重新编译、嵌入启动器及执行缺包校验 |
| 2026-09-07 | 将 PaddleOCR Worker 打包为免 Python 用户组件 | 固定 PaddleOCR/PaddlePaddle/模型版本，解决 oneDNN/PIR 与中文安装路径问题；真实建筑表格截图识别 81 个文字区域；发布包携带三份许可证、5081 项文件哈希清单，并在总发布脚本中增加 `-IncludePaddleOcr` 一键构建与真实进程测试 |
| 2026-09-07 | 完成 LineVision OCR 引擎选择与运行状态 UI | 增加自动/PaddleOCR/Windows 三种模式及本机可用性、模型版本和实际回退结果显示；识别时使用活动进度条，取消会终止独立进程并丢弃半成品；AutoCAD 2024/2026 均 0 警告编译，含真实 PaddleOCR 的 LineVision 测试增至 19 项 |
| 2026-09-07 | 完成工程图 OCR 文字安全规范化 | 仅在数字尺寸语境统一乘号、直径、正负号、比例冒号、全角字符和负号，保留换行与 `OriginalText`，不误改“X轴”等叙述文字；含真实 PaddleOCR 的 LineVision 测试增至 21 项 |

---

## 11. 参考项目

- PaddleOCR：<https://github.com/PaddlePaddle/PaddleOCR>
- PP-OCR 使用说明：<https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/pipeline_usage/OCR.en.md>
- PP-StructureV3：<https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/pipeline_usage/PP-StructureV3.en.md>
- GenOffice：<https://github.com/genspark-ai/genoffice>
- Autodesk ProxyEntity：<https://help.autodesk.com/cloudhelp/2018/ENU/OARX-ManagedRefGuide/files/OREFNET-Autodesk_AutoCAD_DatabaseServices_ProxyEntity.html>
- Autodesk Exploding Entities：<https://help.autodesk.com/cloudhelp/2022/ENU/OARX-DevGuide/files/GUID-88A2E2A3-B8F3-45AE-A484-F366072ADDF6.htm>
- Autodesk 线条和文字转表格说明：<https://www.autodesk.com/support/technical/article/caas/sfdcarticles/sfdcarticles/How-to-convert-text-to-table-in-AutoCAD.html>
- cad-extract：<https://github.com/ricklove/cad-extract>
- Camelot：<https://github.com/camelot-dev/camelot>
- ezdxf：<https://github.com/mozman/ezdxf>
