# 代码修复清单 - 快速参考版

> **修复日期**: 2026-07-28  
> **影响范围**: 3个文件，6个问题  
> **向后兼容**: ✅ 是

---

## 🚀 快速导航

| 文件 | 修改行数 | 优先级 | 测试状态 |
|------|---------|--------|----------|
| PublisherViewModel.cs | ~50行 | 🔴 高 | ⏳ 待测试 |
| CadCompatibilityService.cs | ~80行 | 🟡 中 | ⏳ 待测试 |
| PdfPublisherService.cs | ~20行 | 🟡 中 | ⏳ 待测试 |

---

## 📋 修改清单

### 1️⃣ 修复异步方法签名（高优先级）

**文件**: `PublisherViewModel.cs` 第 747 行

```csharp
// ❌ 修改前
private async void PublishPdf()

// ✅ 修改后  
private async System.Threading.Tasks.Task PublishPdfAsync()
```

**为什么修改**:
- `async void` 会吞掉异常，导致程序崩溃
- 无法追踪异步操作状态
- 不符合 C# 最佳实践

**影响**: 仅 AutoCAD 2022+ 版本，R19 不受影响

---

### 2️⃣ 增加并发发布提示（高优先级）

**文件**: `PublisherViewModel.cs` 第 752-755 行

```csharp
// ❌ 修改前
if (IsPublishing) return;  // 静默返回

// ✅ 修改后
if (IsPublishing)
{
    Status = "发布任务正在进行中，请等待完成后再试。";  // 明确提示
    return;
}
```

**为什么修改**: 用户不知道为什么点击无效

**测试**: 快速双击"发布 PDF"按钮，应显示提示

---

### 3️⃣ 文档切换日志增强（高优先级）

**文件**: `PublisherViewModel.cs` 第 843-851 行

**修改内容**:
- 记录切换前的文档状态
- 添加详细的诊断日志
- 失败时输出更多信息

**为什么修改**: 便于排查"eLayoutNotCurrent"错误

**查看日志**: `%TEMP%\BatchPdfPublisher.publish.log`

---

### 4️⃣ 路径规范化统一（中优先级）

**文件**: `PublisherViewModel.cs` 第 1122-1132 行

```csharp
// ✅ 修改后
private static string NormalizeCadPath(string path)
{
    var normalizedPath = System.IO.Path.GetFullPath(path.Trim());
    // 统一路径分隔符：C:\路径\文件.dwg
    return normalizedPath.Replace(
        System.IO.Path.AltDirectorySeparatorChar, 
        System.IO.Path.DirectorySeparatorChar
    );
}
```

**为什么修改**: 
- 正斜杠/反斜杠混用导致路径比较失败
- 相对路径转绝对路径更可靠

**测试**: 使用带正斜杠的路径测试

---

### 5️⃣ 天正对象检测优化（中优先级）

**文件**: `CadCompatibilityService.cs` 全文

**主要改动**:

1. **性能优化** - 限制检查数量
```csharp
const int MaxEntitiesToCheck = 10000;  // 最多检查1万个实体
if (checkedEntities > MaxEntitiesToCheck) break;
```

2. **新增功能** - 天正对象严重程度分级
```csharp
public enum TianzhengSeverity
{
    None,       // 无天正对象
    Low,        // <10 个
    Medium,     // 10-50 个
    High,       // >50 个
    Unknown     // 检测失败
}
```

**为什么修改**:
- 大图纸检测太慢（>10万实体）
- 可以根据严重程度调整警告级别

**使用示例**:
```csharp
var severity = CadCompatibilityService.DetectTianzhengSeverity(database);
if (severity == TianzhengSeverity.High)
{
    // 显示严重警告
}
```

---

### 6️⃣ 输出目录空间检查（中优先级）

**文件**: `PdfPublisherService.cs` 第 84-106 行

**修改内容**:
- 新增输出目录磁盘空间检查
- 空间不足时记录警告日志

```csharp
var driveInfo = new DriveInfo(outputDrive);
if (driveInfo.AvailableFreeSpace < 512MB)
{
    WritePublishDiagnostic("警告：输出目录磁盘空间不足");
}
```

**为什么修改**: 之前只检查临时目录，不检查输出目录

**测试**: 输出到接近满盘的驱动器

---

## 🧪 测试重点

### 必测项目（5分钟快速测试）

```plaintext
1. ✅ 单文件发布 → 成功
2. ✅ 多文件发布 → 成功
3. ✅ 快速双击发布按钮 → 显示"正在进行中"提示
4. ✅ 天正图纸检测 → 速度正常
5. ✅ 输出到低空间磁盘 → 记录警告
```

### 回归测试（30分钟完整测试）

- 所有现有功能正常工作
- 项目文件正常加载和保存
- 日志文件正常生成
- 没有新的错误或警告

### 性能测试

- 100张图纸发布时间：应该 < 10分钟
- 大图纸天正检测：应该 < 5秒
- 内存占用：应该 < 2GB

---

## 🔍 问题排查指南

### 问题1：发布失败，提示"无法切换文档"

**查看日志**:
```
%TEMP%\BatchPdfPublisher.publish.log
```

**搜索关键词**:
```
文档切换失败
```

**可能原因**:
- CAD 文件被其他程序占用
- 用户手动切换了文档
- AutoCAD 内部错误

---

### 问题2：天正对象检测很慢

**检查**:
- 图纸实体数量（应该自动限制在1万以内）
- 是否有大量代理对象

**解决方案**:
- 代码已优化，如果还慢请报告

---

### 问题3：路径找不到文件

**检查**:
- 是否使用相对路径（现在自动转绝对路径）
- 路径中是否有特殊字符
- 网络驱动器是否连接

**调试**:
```csharp
// 在代码中加断点，查看规范化后的路径
var normalized = NormalizeCadPath(path);
```

---

## 📌 注意事项

### ⚠️ 不向后兼容的改动

**无** - 所有修改都向后兼容

### ✅ 需要用户注意的变化

1. 重复点击发布按钮现在会显示提示（之前静默）
2. 日志文件会有更多诊断信息
3. 输出目录空间不足会记录警告

### 🔄 未来计划

1. **内存优化** - 添加内存监控面板
2. **UI 改进** - 使用标签页减少信息密度
3. **批量处理** - 大图纸分批扫描

---

## 💾 备份与回滚

### 修改前备份
```
BatchPdfPublisher.dll → BatchPdfPublisher.dll.v0.8.2.backup
```

### 如何回滚
```
1. 复制 .backup 文件
2. 重命名为 .dll
3. 重新加载插件
```

---

## 📞 遇到问题？

### 联系方式
- 查看日志：`%TEMP%\BatchPdfPublisher.*.log`
- 提交 Issue：包含日志文件和复现步骤
- 紧急情况：回滚到 v0.8.2

### 常见问题FAQ

**Q: 修改后需要重新编译吗？**  
A: 是的，使用 msbuild 重新编译

**Q: 用户的项目文件会丢失吗？**  
A: 不会，完全兼容

**Q: 性能会变慢吗？**  
A: 不会，天正检测反而更快了

---

## ✅ 检查清单

修改完成后，请确认：

- [ ] 代码已编译无错误
- [ ] 已更新版本号为 v0.8.3
- [ ] 已备份旧版本 DLL
- [ ] 已运行基础测试（5分钟）
- [ ] 已查看日志文件无异常
- [ ] 已更新 README.md（如需要）
- [ ] 已通知团队成员修改内容

---

**最后更新**: 2026-07-28  
**审核状态**: ⏳ 待测试  
**上线状态**: ⏳ 待发布
